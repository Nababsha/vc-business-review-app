# Copy this to .streamlit/secrets.toml for local runs (that file is gitignored).
# On Streamlit Community Cloud, paste this same content into
# your app's Settings -> Secrets instead of committing a file.

ANTHROPIC_API_KEY = "sk-ant-..."
